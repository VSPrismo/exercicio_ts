function multiplicacao(a: number, b: number): number {
  return a * b;
}

function saudacao(nome: string): string {
  return "Olá " + nome;
}

const resultado = multiplicacao(5, 3);
console.log(resultado); 

const saudacaoFinal = saudacao("João");
console.log(saudacaoFinal); 

